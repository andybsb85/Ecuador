<%= form_for(@type_destination) do |f| %>
  <% if @type_destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_destination.errors.count, "error") %> prohibited this type_destination from being saved:</h2>

      <ul>
      <% @type_destination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
